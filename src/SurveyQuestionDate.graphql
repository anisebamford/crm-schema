type SurveyQuestionDate {
  id: String!
  question: DateTime!
  required: Boolean
  rangeMax: DateTime
  rangeMin: DateTime
}

type SurveyAnswerDate {
  question: SurveyQuestionDate!
  answer: DateTime!
}
